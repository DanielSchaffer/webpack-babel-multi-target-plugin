doctype html
html
    head
        title #{locals.title}
        meta(charset="utf-8")

        base(href=".")
        meta(name="viewport", content="width=device-width, initial-scale=1")
        style.
            html, body {
                height: 100%;
                background-color: red;
            }

    body

        h1 #{locals.title}

        app-root
